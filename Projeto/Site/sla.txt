if (dados.botao == "cadastrar")
    {
    await sala.create({
      Nome: dados.sname,
      Capacidade: dados.capacidade,
    });
    }
    else
    {
      await sala.destroy({
        where: {sname}
      });
    }

<button type="submit" class="btn btn-primary" value="editar" name="botao">EDITAR</button>
                        <button type="submit" class="btn btn-primary" value="excluir" name="botao">EXCLUIR</button>